rules:
    - id: python-imports
      message: $MODULE
      languages: [golang]
      severity: WARNING
      metadata:
        category: "import"
      pattern-either:
        - pattern: |
            import (
              "$A"
            )